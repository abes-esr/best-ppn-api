# Synchronisation des sources Avro générées — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendre la génération Avro reproductible sans modifier les schémas, les versions de dépendances ni le contrat Kafka.

**Architecture:** Les fichiers `.avsc` restent les sources de vérité et `avro-maven-plugin` 1.11.2 continue de produire les classes Java suivies dans `src/main/java`. La maintenance committe la sortie canonique actuelle du générateur et complète le guide de développement avec la procédure de régénération et la note de conservation demandée.

**Tech Stack:** Java 21, Maven 3.9.10, Apache Avro 1.12.1, `avro-maven-plugin` 1.11.2, Git.

## Global Constraints

- La branche part de `origin/develop` au commit `628dcd4`.
- Ne modifier ni `src/main/resources/avro/ligne_kbart_convergence.avsc` ni `src/main/resources/avro/ligne_kbart_imprime.avsc`.
- Ne modifier ni la bibliothèque Avro 1.12.1 ni `avro-maven-plugin` 1.11.2.
- Ne déplacer aucune source générée et ne modifier aucune configuration Maven.
- Ne corriger aucun import à la main dans les classes marquées `Autogenerated by Avro`.
- Conserver les tests Spring à zéro exécution comme anomalie préexistante hors périmètre.
- Rédiger les commits et la PR en français avec `Jerome Villiseck <jvk@abes.fr>` comme auteur.

---

### Task 1: Synchroniser et documenter les sources générées

**Files:**
- Modify: `src/main/java/fr/abes/LigneKbartConnect.java`
- Modify: `src/main/java/fr/abes/LigneKbartImprime.java`
- Modify: `README-developpement.md`

**Interfaces:**
- Consumes: les schémas `.avsc` existants et le goal Maven `avro:schema` configuré dans `pom.xml`.
- Produces: deux classes Java identiques à la sortie de `avro-maven-plugin` 1.11.2 et une procédure de maintenance durable.

- [ ] **Step 1: Reproduire la dérive avant correction**

Run:

```powershell
mvn generate-sources
git diff --exit-code -- `
  src/main/java/fr/abes/LigneKbartConnect.java `
  src/main/java/fr/abes/LigneKbartImprime.java
```

Expected: `git diff` termine avec le code `1` et n'affiche que l'ajout ou le réordonnancement d'importations Avro.

- [ ] **Step 2: Vérifier que les schémas restent inchangés**

Run:

```powershell
git diff --exit-code origin/develop -- src/main/resources/avro
```

Expected: code de sortie `0` et aucune sortie.

- [ ] **Step 3: Conserver exactement les classes produites par Maven**

Ne modifier manuellement aucun contenu des deux fichiers Java. Conserver le résultat déjà produit par :

```powershell
mvn generate-sources
```

Le diff attendu est limité à :

```diff
src/main/java/fr/abes/LigneKbartConnect.java
+ import org.apache.avro.generic.GenericArray;
+ import org.apache.avro.util.Utf8;

src/main/java/fr/abes/LigneKbartImprime.java
+ import org.apache.avro.generic.GenericArray;
```

Les autres lignes du diff ne doivent être que le réordonnancement déterministe des imports par le générateur.

- [ ] **Step 4: Ajouter la procédure et la note de conservation**

Dans `README-developpement.md`, compléter la section
`## Schema registry et gestion des versions de schéma` avec ce contenu :

````markdown
### Sources Java générées

Les fichiers `src/main/resources/avro/*.avsc` sont les sources de vérité.
Pendant la phase `generate-sources`, `avro-maven-plugin` génère et remplace
les classes suivantes, qui restent suivies par Git :

- `src/main/java/fr/abes/LigneKbartConnect.java`
- `src/main/java/fr/abes/LigneKbartImprime.java`

Ces classes portent la mention `DO NOT EDIT DIRECTLY` et ne doivent pas être
nettoyées ou réordonnées manuellement, y compris lorsqu'un import paraît
inutile. Après toute modification d'un schéma ou de la version du
générateur, exécuter :

```bash
mvn generate-sources
git diff -- src/main/resources/avro src/main/java/fr/abes/LigneKbartConnect.java src/main/java/fr/abes/LigneKbartImprime.java
```

Le schéma et l'intégralité des sources Java régénérées doivent être commités
ensemble. Une seconde exécution de `mvn generate-sources` doit ensuite
laisser le worktree propre.

> [!NOTE]
> **Conservation et retour arrière — maintenance du 23 juillet 2026**
>
> L'état précédant la resynchronisation est conservé dans Git au commit
> `628dcd4`. Cette maintenance ne modifie aucun fichier `.avsc` ni aucune
> version publiée dans le Schema Registry.
>
> En cas de retour arrière, annuler la PR ou son commit complet. Ne restaurez
> jamais une seule classe générée et ne modifiez pas le Schema Registry :
> les schémas et leurs classes Java doivent toujours provenir d'un même état
> Git. Après restauration, exécuter `mvn generate-sources` et contrôler le
> diff avant tout redéploiement.
````

- [ ] **Step 5: Contrôler le périmètre**

Run:

```powershell
git status --short
git diff --check
git diff --stat origin/develop
git diff origin/develop -- src/main/resources/avro
```

Expected:

- deux classes Java générées modifiées ;
- `README-developpement.md` modifié ;
- les documents de conception et de planification ajoutés ;
- aucun diff sous `src/main/resources/avro` ;
- aucune erreur de whitespace.

- [ ] **Step 6: Committer la synchronisation**

```powershell
git add -- `
  README-developpement.md `
  src/main/java/fr/abes/LigneKbartConnect.java `
  src/main/java/fr/abes/LigneKbartImprime.java
git -c user.name="Jerome Villiseck" -c user.email="jvk@abes.fr" `
  commit -m "maintenance: synchroniser les sources Avro générées"
```

Expected: un commit limité aux deux classes générées et au guide de développement.

### Task 2: Vérifier la reproductibilité et la non-régression

**Files:**
- Verify: `pom.xml`
- Verify: `src/main/resources/avro/ligne_kbart_convergence.avsc`
- Verify: `src/main/resources/avro/ligne_kbart_imprime.avsc`
- Verify: `src/main/java/fr/abes/LigneKbartConnect.java`
- Verify: `src/main/java/fr/abes/LigneKbartImprime.java`

**Interfaces:**
- Consumes: le commit de synchronisation de la tâche 1.
- Produces: les preuves de compilation, de tests et de reproductibilité utilisées dans la PR.

- [ ] **Step 1: Exécuter le build complet**

Run:

```powershell
mvn clean test
```

Expected: `BUILD SUCCESS`, code de sortie `0`, zéro échec dans les tests exécutés. Les trois classes de tests Spring à zéro exécution à cause de Log4j/Kafka restent signalées comme limite préexistante.

- [ ] **Step 2: Vérifier une seconde génération**

Run:

```powershell
mvn generate-sources
git diff --exit-code
git status --short
```

Expected: les deux commandes Git ne signalent aucune modification.

- [ ] **Step 3: Vérifier l'absence de changement des contrats**

Run:

```powershell
git diff --exit-code origin/develop -- src/main/resources/avro pom.xml
git diff --word-diff=plain origin/develop -- `
  src/main/java/fr/abes/LigneKbartConnect.java `
  src/main/java/fr/abes/LigneKbartImprime.java
```

Expected:

- aucune modification des schémas ou de `pom.xml` ;
- uniquement les imports Avro générés dans les deux classes Java.

### Task 3: Publier la PR de maintenance

**Files:**
- Verify: all files changed from `origin/develop`

**Interfaces:**
- Consumes: la branche vérifiée `maintenance/synchroniser-sources-avro`.
- Produces: une PR GitHub vers `develop`, documentée et prête à être relue.

- [ ] **Step 1: Vérifier le périmètre final et l'authentification**

Run:

```powershell
git status --short --branch
git log --oneline origin/develop..HEAD
git diff --stat origin/develop...HEAD
gh --version
gh auth status
```

Expected: worktree propre, trois commits de maintenance documentés, CLI GitHub disponible et authentifié.

- [ ] **Step 2: Pousser la branche**

Run:

```powershell
git push -u origin maintenance/synchroniser-sources-avro
```

Expected: branche distante créée et suivi configuré.

- [ ] **Step 3: Ouvrir une PR en brouillon vers `develop`**

Titre :

```text
Maintenance : synchroniser les sources Avro générées
```

Description :

```markdown
## Contexte

Les classes Java Avro suivies par Git ne correspondaient plus exactement à
la sortie de `avro-maven-plugin` 1.11.2. Chaque build Maven réintroduisait des
imports supprimés manuellement et laissait le worktree modifié.

## Changements

- resynchronisation de `LigneKbartConnect` et `LigneKbartImprime` avec la
  sortie canonique du générateur ;
- documentation de la source de vérité et de la procédure de régénération ;
- ajout d'une note de conservation et de retour arrière.

## Absence d'impact fonctionnel

- aucun changement des schémas `.avsc` ;
- aucune modification de version Avro ou Maven ;
- aucun changement des champs, du code de sérialisation ou du contrat Kafka.

## Validation

- `mvn clean test` ;
- seconde exécution de `mvn generate-sources` sans diff Git ;
- vérification explicite de l'absence de diff sous `src/main/resources/avro`
  et dans `pom.xml`.

## Limite préexistante

Trois classes de tests Spring restent à zéro exécution à cause de
l'initialisation Log4j/Kafka locale. Cette anomalie n'est pas introduite ni
modifiée par cette maintenance.
```

Expected: PR en brouillon, base `develop`, head `maintenance/synchroniser-sources-avro`.
